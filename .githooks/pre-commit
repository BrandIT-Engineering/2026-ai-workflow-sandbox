#!/bin/sh
branch=$(git symbolic-ref --short HEAD 2>/dev/null)

case "$branch" in
  main|dev)
    echo "Bu branch'te commit yasak. Önce 'git start <kisa-isim>' çalıştır." >&2
    exit 1
    ;;
esac

exit 0
